import styled from "styled-components";

export const Menu = styled.header`

    header{
        display: flex;
        height: 20vh;
    }
    @media (width <= 600px) {
        height: auto;
    }
    
`;